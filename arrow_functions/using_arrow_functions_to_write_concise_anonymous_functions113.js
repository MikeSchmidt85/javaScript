// Using Arrow Functions to Write Concise Anonymous Functions

// Whenever you have an anonymous function you can convert it into an arrow function

var magic = function() {
    return new Date();

};

//-------------------------------------------

const magic = () => newDate();

// These two seperate codes are essentially the same, the top is an anonymous function while the bottom is an arrow function