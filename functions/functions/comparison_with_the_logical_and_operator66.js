// Comaprison with the Logical and Operator

function testLogicalAnd(val) {
    // Only change code below this line

    if (val <= 50 && val >= 25) { // This is checking if both statements are true
        // You can combine nested for loops by just using the && signs

        return "Yes";
}

// Only change code above this line
    return "No";
}

// Change this value to test
// console.log(testLogicalAnd(10)); // This will print out No (This is outta the range)
// console.log(testLogicalAnd(30)); // this will print out Yes (this is inside the range)
