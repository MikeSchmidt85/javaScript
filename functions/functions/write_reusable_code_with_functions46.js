// Write Reusable Code with Functions

// Example

function ourReusableFunction() {  // ourResusableFunction is the function name
    console.log("Heyya, World");  // Anything inside the curly bracket will run (2nd step)
}

ourReusableFunction();  //Anytime the function is called ^^ (1st step)
ourReusableFunction();  //Anytime the function is called ^^ (1st step)
ourReusableFunction();  //Anytime the function is called ^^ (1st step)

// Only change code below this line

function reusableFunction() {
    console.log("Hello World!!");

}

reusableFunction();

function helloPauline() {
    console.log("Daddy loves you!");
}
helloPauline();
