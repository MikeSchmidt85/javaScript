// Generate Random fractions

function randomFraction() {

    return Math.random();  // This chooses a random number between 0-.9999
                           // It could be 0 but not 1
}

console.log(randomFraction());