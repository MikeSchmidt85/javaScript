// Using the parseInt Function

// This uses parseInt to convert a string to a number

function convertToIngeter(str) {

        return parseInt(str);
}

console.log(convertToIngeter("56")); // This will print out 56 as a number not a string